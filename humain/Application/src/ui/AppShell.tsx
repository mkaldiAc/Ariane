import type {ReactNode} from 'react';import {BookOpen,Boxes,CheckCircle2,Database,GitBranch,Menu,Network,UserRound} from 'lucide-react';
export type View='guide'|'programmes'|'captures'|'structures'|'validations';const nav=[[BookOpen,'Guide de captation','guide'],[Database,'Programmes','programmes'],[Boxes,'Captations','captures'],[GitBranch,'Structures','structures'],[CheckCircle2,'Validations','validations']] as const;
const Brand=()=> <div className="aiguillon-brand"><span className="aiguillon-brand__mark"><Network/></span><span>ARIANE<small>CAPTATION PATRIMONIALE</small></span></div>;
export function AppShell({children,view,onNavigate}:{children:ReactNode;view:View;onNavigate:(v:View)=>void}){return <div className="aiguillon-shell"><aside className="aiguillon-sidebar"><Brand/><nav className="aiguillon-nav">{nav.map(([Icon,label,id])=><button key={id} className="aiguillon-nav__item" aria-current={view===id?'page':undefined} onClick={()=>onNavigate(id)}><Icon/>{label}</button>)}</nav><div className="aiguillon-user"><span>AR</span><div><b>Application ARIANE</b><small>Documentation & opérations</small></div></div></aside><header className="aiguillon-topbar"><button className="icon-button"><Menu/></button><Brand/><span className="aiguillon-topbar__spacer"/><button className="icon-button"><UserRound/></button></header><main className="aiguillon-main"><div className="aiguillon-content">{children}</div></main><nav className="ariane-mobile-nav">{nav.map(([Icon,label,id])=><button key={id} aria-current={view===id?'page':undefined} onClick={()=>onNavigate(id)}><Icon/><span>{label}</span></button>)}</nav></div>}